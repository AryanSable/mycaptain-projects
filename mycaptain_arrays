#include <iostream>

using namespace std;

int main()
{ int i,num;

cout<<"Enter the number of elements"<<endl;
cin>>num;
int arr[num];
  cout<<"Enter the elements:"<<endl;
    for(i=0;i<num;i++){
        cin>>arr[i];
        
    }
    cout<<"Entered elements: "<<endl;
    for(i=0;i<num;i++){
        cout<<arr[i]<<endl;
    }

    return 0;
    }
